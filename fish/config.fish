if not status --is-interactive
end

abbr -a gap git add -p
abbr -a ga. git add .
abbr -a gca git commit --amend
abbr -a gc git commit
abbr -a gd git diff
abbr -a gs git status
abbr -a gl git log
abbr -a gco git checkout
abbr -a gu gitui
abbr -a gl1 git log --oneline